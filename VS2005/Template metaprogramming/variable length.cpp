#include <iostream>
#include <stdlib.h>
#include <list>
using namespace std;
/*
//需要支持C++11的编译器才能使用可变模板参数
void printStuff( )
{
	cout << endl;
}

template<typename Tfirst, typename... Trest>
void printStuff( Tfirst first, Trest ... rest )
{
	cout << first << ' ';
	printStuff( rest ... );
}

int main1(int argc, char **argv)
{
	printStuff( 3, 3.3, "foo", 42 );

	return 0;
}
*/
